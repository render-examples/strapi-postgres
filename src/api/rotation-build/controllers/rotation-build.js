'use strict';

/**
 * rotation-build controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::rotation-build.rotation-build');
